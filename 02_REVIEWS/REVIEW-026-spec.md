# REVIEW-026: SPEC-026 对抗审查记录

- 审查对象: `04_SPECS/SPEC-026-DiskScanMerge.md`（TASK-026）
- 审查方式: **降级独立审查 pass（环境无真实子智能体工具，双视角隔离审查兜底）**
- 日期: 2026-08-08

## Pass 1（架构视角）
1. **[P0] 遍历深度语义漂移**：`scan_large_files` 全深递归；`scan_dir_usage` max_depth=3。合并单遍历若统一 max_depth=3 会**漏掉深层大文件**（如 Videos 下 5 级深的大视频）；若不限深，目录聚合语义变化。修订：**单遍历不限深**，目录聚合仅对深度 ≤3 的父目录计入（保持原语义），或明确放宽为全深 Top100 并更新说明。倾向后者（信息更全），但需与"3 层"UI 文案一致。
2. **[P1] 事件收敛成本**：新事件名 `disk-user-*` + 命令合并 + useDisk 重构，改动面大；收益（省一次遍历）值得。保留方案，但**数据事件名保留**（disk-large-files/disk-dir-usage）降前端改动。
3. **[P2] 取消语义**：单遍历共享 cancel flag，与旧行为等价，OK。

## Pass 2（代码/边界视角）
1. **[P1] 等价性测试粒度**：fixture 对比需覆盖：阈值过滤、风险分级、Temp/hive/node_modules 跳过、目录聚合 Top100 排序。SPEC 已提等价测试，补全断言清单。
2. **[P2] delete_large_files 依赖 scan_root**：合并后 `start_user_scan` 仍写 scan_root，删除命令不变，OK。

## 结论
**有条件通过**。需修订：① 明确遍历深度策略（不限深 + 目录聚合语义）；② 数据事件名保留降低前端改动。

## 采纳记录
| 意见 | 采纳 | 处理 |
|---|---|---|
| P0 深度语义 | 采纳 | SPEC 修订：单遍历不限深；目录聚合深度 ≤3 保持原语义 |
| P1 数据事件名保留 | 采纳 | SPEC 修订：仅进度/done/error 收敛为 disk-user-* |
| P2 等价测试清单 | 采纳 | SPEC 修订：补断言清单 |
